import 'package:flutter/material.dart';

class GenerationRankingScreen extends StatelessWidget {
  const GenerationRankingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('GenerationRankingScreen'),
    );
  }
}
